#include <iostream>
#include <map>
#include <vector>

class Intruso{

private:
std::map< char, std::string >  
std::vector< std::map<char,string>> date;

public:

intruso()=default;


void set_senha_vaza(std::string vazou){
  
std::string filtro;
std::string t;
int b=0;
  
while(int i=0; i!=29; i++){if(vazou[i]!=' ') filtro+=vazou[i];}
 
for(int a=0; a<9; a+2){
t = filtro[a]+filtro[a+1];
senha.insert(41+b,t);
b++;
}
date.push_back(senha);

}




std::string crack_senha(){
  
std::string str;
  
(auto ptr : date){
 

}

return str;
  
  
}


}